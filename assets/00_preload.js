sessionStorage.clear();


/*
// Delegación de eventos - funciona con elementos dinámicos
document.addEventListener("click", function (e) {

    const details = e.target.closest("details.custom-collapse");

    if (details) {
        e.stopPropagation();
    }

}, true);

*/